//export const registerModules = (modules , state) =>{
//  for(const [name , module] of Object.entries(modules)){    
//    state[name] = module.state;
//    if(module.modules){
//      registerModules(module.modules , module.state);
//    }
//  }
//  return state;
//}
